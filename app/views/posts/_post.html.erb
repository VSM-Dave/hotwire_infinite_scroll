<%= content_tag(:div, id: dom_id(post), class: "post card mb-3") do %>
  <div class="card-body">
    <%= post.body %>
  </div>
  <div class="card-footer">
    <%= post.user.name %> <%= local_time_ago(post.created_at) %>
  </div>
<% end %>